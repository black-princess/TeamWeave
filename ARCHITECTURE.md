# TeamWeave Architecture & Flow Diagrams

## System Architecture

```
???????????????????????????????????????????????????????????????????????
?                         TEAMWEAVE SYSTEM                             ?
?                    Hackathon Team Matcher Portal                    ?
???????????????????????????????????????????????????????????????????????

????????????????????????????????????????????????????????????????????????
?  APPLICATION LAYER (Program.cs)                                      ?
?  • Demo Application                                                  ?
?  • User Interaction                                                  ?
?  • Data Visualization                                                ?
????????????????????????????????????????????????????????????????????????
                                 ?
????????????????????????????????????????????????????????????????????????
?  SERVICE LAYER                                                       ?
?  ??????????????????????  ??????????????????????                    ?
?  ? Participant Service?  ?  Team Service      ?                    ?
?  ? • Register         ?  ? • Create Teams     ?                    ?
?  ? • Add Skills       ?  ? • Add Members      ?                    ?
?  ? • Add Interests    ?  ? • Move Between     ?                    ?
?  ? • Get Participants ?  ? • Lock/Unlock      ?                    ?
?  ? • Filter by Skill  ?  ? • Audit Logging    ?                    ?
?  ??????????????????????  ??????????????????????                    ?
?  ??????????????????????  ??????????????????????                    ?
?  ?Clustering Service  ?  ?SkillVectorEncoder  ?                    ?
?  ? • K-means Cluster  ?  ? • Encode Skills    ?                    ?
?  ? • Skill Balance    ?  ? • Distance Calc    ?                    ?
?  ? • Diversity Score  ?  ? • Similarity Calc  ?                    ?
?  ? • Convergence Chk  ?  ? • Category Dist    ?                    ?
?  ??????????????????????  ??????????????????????                    ?
????????????????????????????????????????????????????????????????????????
                                 ?
????????????????????????????????????????????????????????????????????????
?  UTILITY LAYER                                                       ?
?  ??????????????????????  ??????????????????????                    ?
?  ?SkillRepository     ?  ? DataExporter       ?                    ?
?  ? • Skill Catalog    ?  ? • Export CSV       ?                    ?
?  ? • Search Skills    ?  ? • Format Data      ?                    ?
?  ? • Categories       ?  ? • Participant CSV  ?                    ?
?  ? • Get By Name/ID   ?  ? • Team CSV         ?                    ?
?  ??????????????????????  ? • Audit Log CSV    ?                    ?
?                           ??????????????????????                    ?
????????????????????????????????????????????????????????????????????????
                                 ?
????????????????????????????????????????????????????????????????????????
?  DATA MODEL LAYER                                                    ?
?  ????????????????  ????????????????  ????????????????              ?
?  ? Participant  ?  ?    Skill     ?  ?   Team       ?              ?
?  ? • Id         ?  ? • Id         ?  ? • Id         ?              ?
?  ? • Name       ?  ? • Name       ?  ? • Name       ?              ?
?  ? • Email      ?  ? • Category   ?  ? • EventId    ?              ?
?  ? • Status     ?  ? • CreatedAt  ?  ? • Status     ?              ?
?  ? • Skills[]   ?  ?              ?  ? • IsLocked   ?              ?
?  ? • Interests[]?  ?              ?  ? • Members[]  ?              ?
?  ? • Teams[]    ?  ?              ?  ?              ?              ?
?  ????????????????  ????????????????  ????????????????              ?
?  ????????????????  ????????????????  ????????????????              ?
?  ?ParticipantSk ?  ? TeamMember   ?  ?  AuditLog    ?              ?
?  ? • Id         ?  ? • Id         ?  ? • Id         ?              ?
?  ? • ParticipId ?  ? • TeamId     ?  ? • ChangeType ?              ?
?  ? • SkillId    ?  ? • PartId     ?  ? • Actor      ?              ?
?  ? • Proficiency?  ? • IsLocked   ?  ? • OldValue   ?              ?
?  ? • AddedAt    ?  ? • AddedAt    ?  ? • NewValue   ?              ?
?  ????????????????  ????????????????  ? • Timestamp  ?              ?
?                                       ????????????????              ?
?  ????????????????                                                   ?
?  ?ClusteringRun ?                                                   ?
?  ? • Id         ?                                                   ?
?  ? • EventId    ?                                                   ?
?  ? • ExecutedAt ?                                                   ?
?  ? • TeamsGen   ?                                                   ?
?  ? • Parameters ?                                                   ?
?  ? • Status     ?                                                   ?
?  ????????????????                                                   ?
????????????????????????????????????????????????????????????????????????
```

---

## Data Flow Diagram

```
                           REGISTRATION FLOW
                           ================

     Participant
          ?
    [RegisterParticipant]
          ?
    Create Participant Entity
          ?
    Add Skills & Interests
          ?
    ParticipantService.AddSkillToParticipant()
          ?
    Store in Participant.Skills Collection
          ?
    Profile Status = "Submitted"


                         CLUSTERING FLOW
                         ===============

    Participants[]
          ?
    ClusteringService.ClusterParticipants()
          ?
    [Initialize Centroids]
          ?
    For each iteration:
         ?? Encode Participant Skills ? Vector[]
         ?? Calculate Distance to Centroids
         ?? Assign to Nearest Cluster
         ?? Recalculate Centroids
         ?? Check Convergence
          ?
    Return: List<List<Participant>>
          ?
    Each list = one team


                      TEAM CREATION FLOW
                      ==================

    Clusters[]
          ?
    For each cluster:
         ?? TeamService.CreateTeam()
         ?? For each participant:
         ?   ?? TeamService.AddParticipantToTeam()
         ?   ?? Create TeamMember entity
         ?   ?? Log audit event
         ?? Set status = "Draft"
          ?
    Teams[] with Members


                   TEAM ADJUSTMENT FLOW
                   ====================

    Move Participant Request
          ?
    [Check if locked]
          ?
    No ? Continue
    Yes ? Throw Exception
          ?
    Remove from Source Team
          ?
    Add to Destination Team
          ?
    Create AuditLog entry
          ?
    Update Team.UpdatedAt


                        EXPORT FLOW
                        ===========

    Participants[]
          ?
    DataExporter.ExportParticipantsToCSV()
          ?
    For each participant:
         ?? Extract data
         ?? Format as CSV row
         ?? Add to StringBuilder
          ?
    Return: CSV String
          ?
    Write to File
```

---

## Clustering Algorithm Flow

```
        CLUSTERING ALGORITHM (K-means)
        ==============================

    INPUT: Participants[], Team Size, Max Iterations
         ?
    [Step 1: Initialize]
         ?? Calculate num_clusters = Participants.Count / TeamSize
         ?? Select random centroids from participants
         ?? Convert to skill vectors
         ?
    [Step 2: Iterate]
    ??????????????????????????????????????
    ?  For each iteration (0 to max):    ?
    ?  ????????????????????????????????  ?
    ?  ? [Assign Step]                ?  ?
    ?  ? For each participant:        ?  ?
    ?  ?  ?? Encode skills to vector  ?  ?
    ?  ?  ?? Calculate distance to    ?  ?
    ?  ?  ?  all centroids            ?  ?
    ?  ?  ?? Assign to nearest        ?  ?
    ?  ????????????????????????????????  ?
    ?  ????????????????????????????????  ?
    ?  ? [Update Step]                ?  ?
    ?  ? For each cluster:            ?  ?
    ?  ?  ?? Get all assigned points  ?  ?
    ?  ?  ?? Calculate mean vector    ?  ?
    ?  ?  ?? Update centroid          ?  ?
    ?  ????????????????????????????????  ?
    ?  ????????????????????????????????  ?
    ?  ? [Convergence Check]          ?  ?
    ?  ? If centroid movement < 0.0001?  ?
    ?  ?  ?? BREAK (converged)        ?  ?
    ?  ????????????????????????????????  ?
    ??????????????????????????????????????
         ?
    [Step 3: Group]
         ?? Create empty lists for each cluster
         ?? Add participants to their clusters
         ?? Remove empty clusters
         ?
    OUTPUT: List<List<Participant>>
```

---

## Vector Encoding & Distance Calculation

```
        SKILL VECTOR ENCODING
        ======================

    Participant Skills: {React:5, JavaScript:4}
         ?
    Skill Index: {React: 0, JavaScript: 1, ..., Total: N}
         ?
    Create vector of size N, initialized to 0.0
         ?
    For each skill:
      vector[index] = proficiency / 5.0
         ?
    Result: [1.0, 0.8, 0.0, ..., 0.0]
         ?
    This vector represents the participant's skill profile
    normalized to [0, 1] range


        EUCLIDEAN DISTANCE
        ==================

    Vector A: [1.0, 0.8, 0.5]
    Vector B: [0.9, 0.7, 0.6]
         ?
    distance = ?[(1.0-0.9)² + (0.8-0.7)² + (0.5-0.6)²]
    distance = ?[0.01 + 0.01 + 0.01]
    distance = ?0.03 ? 0.173
         ?
    Used by K-means to find nearest cluster


        COSINE SIMILARITY
        =================

    Vector A: [1.0, 0.8]
    Vector B: [1.0, 0.8]
         ?
    dotProduct = (1.0×1.0) + (0.8×0.8) = 1.64
    magA = ?(1.0² + 0.8²) = 1.281
    magB = ?(1.0² + 0.8²) = 1.281
         ?
    similarity = 1.64 / (1.281 × 1.281) = 1.0
         ?
    Range: [-1, 1], where 1 = identical
```

---

## Team Balancing Algorithm

```
        SKILL DIVERSITY BALANCING
        ==========================

    INPUT: Participants[], Team Size
         ?
    [Sort by Diversity]
    For each participant:
      ?? Count unique skill categories
      ?? Sum proficiency levels
      ?? Score = categories × avg_proficiency
         ?
    Sort descending by score
         ?
    [Greedy Selection]
    For each team to form:
      ?? Initialize empty team
      ?? For each participant (sorted):
      ?   ?? Add to team
      ?   ?? Count teams [teamSize] reached?
      ?   ?? Check balance requirements
      ?? If balanced: keep team
      ?? Else: try next participant
         ?
    OUTPUT: List<List<Participant>> (balanced teams)
```

---

## Audit Trail Recording

```
        AUDIT LOG CREATION
        ==================

    Action: "Move Participant from Team A to Team B"
         ?
    Trigger: TeamService.MoveParticipantBetweenTeams()
         ?
    Create AuditLog entry:
    {
      Id: auto-increment,
      ChangeType: "ParticipantMoved",
      TeamId: B,
      ParticipantId: 5,
      OldValue: "Team A",
      NewValue: "Team B",
      Actor: "Organizer Name",
      Timestamp: DateTime.Now
    }
         ?
    Store in auditLogs collection
         ?
    Can be queried and exported anytime


        AUDIT TRAIL EVENTS
        ==================

    Tracked Events:
    ?? ParticipantAdded (to team)
    ?? ParticipantMoved (between teams)
    ?? ParticipantLocked (in team)
    ?? TeamLocked (entire team)
    ?? TeamUnlocked (unlock team)
    ?? (Extensible for custom events)
```

---

## Service Dependencies

```
        SERVICE DEPENDENCY GRAPH
        =========================

                        Program.cs (Demo)
                             ?
        ???????????????????????????????????????????
        ?                    ?                    ?
    ParticipantService   TeamService      ClusteringService
        ?                    ?                    ?
    Models:             Models:          SkillVectorEncoder
    • Participant       • Team           • Math operations
    • Skill             • TeamMember     • Vector encoding
    • ParticipantSkill  • AuditLog       • Distance calc
        ?                    ?                    ?
    SkillRepository      DataExporter
    • Skill catalog      • CSV export
    • Category filter    • Formatting
```

---

## Database Schema (Conceptual - Ready for Entity Framework)

```
    Participants
    ?? Id (PK)
    ?? Name
    ?? Email
    ?? ProfileStatus
    ?? CreatedAt
    ?? UpdatedAt
         ? (1:M)
    ParticipantSkills
    ?? Id (PK)
    ?? ParticipantId (FK)
    ?? SkillId (FK)
    ?? ProficiencyLevel
    ?? AddedAt
         ? (M:1)
    Skills
    ?? Id (PK)
    ?? Name
    ?? Category
    ?? CreatedAt


    Teams
    ?? Id (PK)
    ?? Name
    ?? EventId
    ?? Status
    ?? IsLocked
    ?? CreatedAt
    ?? UpdatedAt
         ? (1:M)
    TeamMembers
    ?? Id (PK)
    ?? TeamId (FK)
    ?? ParticipantId (FK)
    ?? IsLocked
    ?? AddedAt
         ? (M:1)
    Participants


    ClusteringRuns
    ?? Id (PK)
    ?? EventId
    ?? ExecutedAt
    ?? TeamsGenerated
    ?? Parameters
    ?? Status


    AuditLogs
    ?? Id (PK)
    ?? ParticipantId (FK)
    ?? TeamId (FK)
    ?? ChangeType
    ?? Actor
    ?? OldValue
    ?? NewValue
    ?? Timestamp
```

---

## Deployment Architecture

```
        DEPLOYMENT FLOW
        ===============

    Local Development
         ? (dotnet build/run)
    Console Application
         ? (Add Web Layer)
    ASP.NET MVC
         ? (Add Database)
    SQL Server Backend
         ? (Add APIs)
    REST Web APIs
         ? (Deploy)
    IIS / Azure App Service
         ?
    Production System
         ?? Web Frontend
         ?? API Backend
         ?? SQL Database
         ?? Logging Service
         ?? Monitoring


        SCALE-OUT CONSIDERATIONS
        ========================

    Current: In-Memory
         ?
    Phase 1: SQL Database
         ?? Entity Framework
         ?? Indexed queries
         ?? Connection pooling
         ?
    Phase 2: Multi-Server
         ?? Load balancer
         ?? Distributed cache
         ?? Message queue for clustering
         ?
    Phase 3: Cloud-Native
         ?? Azure/AWS deployment
         ?? Auto-scaling
         ?? Managed database services
```

---

## Error Handling Flow

```
        ERROR HANDLING
        ==============

    User Action
         ?
    Input Validation
         ?? Check required fields
         ?? Validate data types
         ?? Check constraints
         ?
    If Invalid ? ArgumentException
         ?
    Service Operation
         ?? Execute business logic
         ?? Check state
         ?
    If Failed ? InvalidOperationException
         ?
    Log error (if applicable)
         ?
    Return error message to user
         ?
    User sees clear error description
```

---

## Workflow Sequence

```
        COMPLETE WORKFLOW
        =================

    1. Registration Phase
       Participant ? RegisterParticipant ? Stored
                  ? AddSkills ? Skills associated
                  ? AddInterests ? Interests recorded
       ? Status: Submitted

    2. Organization Phase
       Organizer ? ConfigureEvent (size, constraints)
       ? Status: Ready

    3. Clustering Phase
       System ? ClusterParticipants() ? Algorithm runs
              ? Convergence reached
              ? Clusters returned
       ? Status: Suggested teams

    4. Team Formation Phase
       System ? CreateTeams() ? Teams created
              ? AddMembers() ? Participants assigned
       ? Status: Draft

    5. Review Phase
       Organizer ? ViewTeamComposition ? See assignments
                 ? CheckBalance ? Verify diversity
       ? Status: Reviewed

    6. Adjustment Phase (Optional)
       Organizer ? MoveParticipant() ? Adjust manually
                 ? Audit logged ? Change recorded
       ? Status: Adjusted

    7. Finalization Phase
       Organizer ? LockTeams() ? Teams finalized
       ? Status: Locked

    8. Export Phase
       System ? ExportData() ? CSV generated
              ? FileWrite() ? File saved
       ? Status: Exported

    9. Event Phase
       Event ? HackathonRuns ? Teams work together
       ? Status: Active

    10. Completion Phase
        Teams ? Submit Results ? Event ends
        ? Status: Completed
```

---

This architecture is designed for:
- ? **Scalability**: Ready for database and cloud deployment
- ? **Maintainability**: Clear separation of concerns
- ? **Extensibility**: Easy to add new features
- ? **Reliability**: Proper error handling
- ? **Auditability**: Complete change tracking
- ? **Performance**: Optimized algorithms

